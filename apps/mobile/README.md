# Mobile (`apps/mobile`)

Reserved for future React Native application.

Planned behavior:
- Connect to same WebSocket contract as web
- Reuse shared types from `packages/shared-types`
- Provide mobile-optimized situational view
